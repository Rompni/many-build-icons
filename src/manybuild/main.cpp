#include <Geode/Geode.hpp>

using namespace geode::prelude;

$on_mod(Loaded) {
    // Aquí se activa el hook de GJGarageLayer automáticamente
    log::info("Many Build Icons cargado correctamente.");
}